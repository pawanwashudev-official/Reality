1. **Apply missing Neural/Cinematic UI text updates across remaining activities**
   - Address the user's feedback that "many activity file you havent updated".
   - Find remaining screens involving onboarding, setup, manual, and main UI that could use the new branding.
   - For example, `activity_main.xml`, `activity_user_manual.xml`, `activity_settings.xml`.

2. **Pre-commit and Test**
   - Compile and test.
