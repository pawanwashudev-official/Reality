1. Update `app/src/main/java/com/neubofy/reality/utils/UpdateManager.kt`:
   - Create `checkForBetaUpdates` to query `https://api.github.com/repos/pawanwashudev-official/Reality/releases` and pick the first release where `prerelease == true`.
   - Update `fetchGitHubReleases` method to handle array response and find the beta.

2. Update `app/src/main/java/com/neubofy/reality/services/AppBlockerService.kt`:
   - In `handleBlock()`, call `pressHome()` unconditionally before starting the `BlockActivity`. This will ensure the app is closed, satisfying "App blocker overlay should be over any layer of app opening even in split screen or pop up and it should close app not just overlay."
   - In `checkUrl()`, add `performGlobalAction(GLOBAL_ACTION_HOME)` before launching the `BlockActivity` for websites.

3. Update `app/src/main/res/layout/activity_about.xml` and `app/src/main/java/com/neubofy/reality/ui/activity/AboutActivity.kt`:
   - Add a new Card for "Check Beta Updates" just below the main update card.
   - Add a Card/Button for "Raise an Issue" that opens a custom form dialogue or opens the GitHub issues page. Let's create an Intent that opens `https://github.com/pawanwashudev-official/Reality/issues/new`. Or build a tiny dialog that opens `https://github.com/pawanwashudev-official/Reality/issues/new?title=...&body=...` via browser, since doing a direct API call requires GitHub PAT which we shouldn't hardcode. The user asked to "give a button which will open a form where user describe issue and in submit it will creat issue in our github repo". A custom dialog in-app with title and body, when "Submit" is clicked, we launch an Intent to open the URL with pre-filled title/body.

4. Update `ABOUT.md`:
   - Actually the app downloads `ABOUT.md` from GitHub main branch! But I can also update it if it's in the repo, let's check if it exists locally.
